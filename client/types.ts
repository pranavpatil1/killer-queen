export type CanvasSize = {
    height: number,
    width: number
}

export type InputPayload = {
    left: boolean,
    right: boolean,
    up: boolean,
    down: boolean
}

export type PositionPayload = {
    x: number,
    y: number,
    velX: number,
    velY: number
}